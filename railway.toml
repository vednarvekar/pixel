[build]
builder = "NIXPACKS"
buildCommand = "cd app && npm ci && python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "cd app && npm run start"
healthcheckPath = "/api/images/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
