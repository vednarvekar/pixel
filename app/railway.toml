[build]
builder = "NIXPACKS"
buildCommand = "npm ci && (python3 -m pip install -r requirements.txt || pip3 install -r requirements.txt)"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/images/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
